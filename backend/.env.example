# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database
DATABASE_PATH=./data/kiosk.db

# Session
SESSION_SECRET=change-this-to-a-random-secret-in-production

# Admin Credentials (default)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# n8n Integration API Key (for WhatsApp coupon system)
# Generate with: openssl rand -base64 32
# This key is used to authenticate requests from n8n workflows to the backend API
N8N_API_KEY=change-this-to-a-secure-random-key

# n8n Webhook URL (for forwarding WhatsApp messages from backend to n8n)
# On Pi, n8n runs on localhost:5678
N8N_WEBHOOK_URL=http://localhost:5678/webhook/whatsapp

# WhatsApp Configuration (for coupon system)
# Phone number in international format without + (e.g., 905551234567 for Turkey)
# Used to generate WhatsApp deep links for coupon tokens
WHATSAPP_NUMBER=905551234567

# WhatsApp Cloud API Credentials (Meta Business API)
# Get these from: https://developers.facebook.com/apps
# IMPORTANT: Keep these secret! Never commit to git.
WHATSAPP_ACCESS_TOKEN=your_permanent_access_token_here
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id_here
WHATSAPP_APP_SECRET=your_app_secret_here
WHATSAPP_VERIFY_TOKEN=create_random_string_for_webhook_verification

# Timezone Configuration
# Set to Europe/Istanbul for correct rate limit resets and scheduled jobs
# This ensures midnight calculations and cron jobs run at the correct local time
TZ=Europe/Istanbul

# Google Sheets (optional - configure in admin panel)
GOOGLE_SHEETS_CREDENTIALS=

# File Upload
# UPLOAD_DIR is now managed by backend/src/config/paths.ts
# Uncomment and set only if you need a custom upload directory
# UPLOAD_DIR=/custom/path/to/uploads
MAX_VIDEO_SIZE=52428800
MAX_IMAGE_SIZE=5242880
